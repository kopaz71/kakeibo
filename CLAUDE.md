# カード明細家計簿

楽天カードの明細CSVを取り込み、自動でカテゴリ分けして集計する**スマホ専用**の家計簿Webアプリ。
GitHub Pages（`main` ブランチのルート）で公開し、ホーム画面に追加して使う（PWA）。
利用者は1人。PCでは使わない。

## いちばん大事なルール

1. **明細データを壊さない・失わせない。** データは利用者のスマホの `localStorage` にしかない。
   保存形式を変えるときは、必ず読み込み時の移行処理を書く（「データ形式」の項を参照）。
2. **`index.html` などを変えたら、`sw.js` の `CACHE` の数字を1つ上げる**（例: `kakeibo-v11` → `kakeibo-v12`）。
   上げないと、スマホに古い版がキャッシュされたまま更新されない。
   - `sw.js` はブラウザのキャッシュを通さずに毎回サーバーへ確認する（`fetch(..., {cache:"no-cache"})`、インストール時は `cache:"reload"`、登録は `updateViaCache:"none"`）。
     GitHub Pages が「10分ためてよい」と返すため、これがないとスマホで古い版が出続けることがあった
   - アプリを開いたとき・裏から戻ったとき（`visibilitychange`）に更新を確認し、新しい版に切り替わったら（`controllerchange`）
     バナー「新しい版があります｜更新する」（`S.updateReady`）を出す。**勝手に読み込み直さない**（入力中の内容を消さないため）。
     「更新する」は保存待ちを `flush()` してから `location.reload()`
   - 設定の一番下に「アプリの版：kakeibo-v◯◯」（`showAppVersion`。入っているキャッシュの名前）。PCとスマホで同じ版か比べられる
3. **明細やお金のデータをリポジトリに入れない。** 公開リポジトリなので、CSV・バックアップJSON・実際の明細を
   コミットしない（`.gitignore` 済み）。テストやサンプルには架空のデータを使う。
4. **外部に通信しない。** 外部のフォント・CDN・解析ツール・APIを追加しない。家計のデータが外に出ないことが前提。
   読み込むのは同じリポジトリ内のファイルだけ。

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体。HTML・CSS・JavaScriptをすべてこの1ファイルに書く（ビルド工程なし、ライブラリなし） |
| `sw.js` | Service Worker。オフライン起動用のキャッシュ（ネット優先・失敗時にキャッシュ） |
| `manifest.json` | ホーム画面に追加したときの名前・アイコン・色 |
| `icons/` | アプリアイコン（ノートと鉛筆のデザイン、オレンジ背景 `#e8783a`）。192・512・180（apple-touch）・maskable 512 |
| `README.md` | 利用者向けの公開・インストール・更新の手順 |

ファイルは分割せず、`index.html` 1ファイルの構成を保つ。

## 画面構成

下部の固定タブで4画面を切り替える（`TAB_ORDER`）。

- **概要**（`renderOverview`）。一番上の「月｜年」の切り替え（`.seg`、`S.ov`）で `renderMonth` と `renderYear` を出し分ける
  - 月の表示（`renderMonth`）
    - 上部に「前月｜当月の支出（大きく）｜次月」の帯（`.mstrip`）。左右をタップで月を移動。
      中央をタップすると月のプルダウン（透明な `select.mpick` を重ねている。選択肢は `monthOptions`：明細のある月と今の月、新しい順。
      iPhoneのプルダウンは幅が狭く折り返すので、選択肢は「2025年10月」のように月名だけにする）
    - 予算を設定しているときは、帯の下に「予算｜残り（超えたら赤で ▲¥…）」と横長のバー（`budgetCard`）
    - 件数・前月比・未分類の件数
    - カテゴリ別（`catPanel`）：ドーナツ型の円グラフ（`catDonut`。扇形の色はカテゴリのアイコンの色、中央に合計）と一覧。扇形・一覧をタップでそのカテゴリの明細へ
      - 一覧は**1行にまとめてコンパクトに**（利用者の希望）：「アイコン 名前｜予算のバー｜金額（下に小さく 残り/▲。予算なしなら割合）」
      - 最初は**上位5件だけ**。予算を超えたカテゴリは必ず出す。残りは「ほか◯件を表示」で開く（`S.catAll`）
    - 月別の支出（直近12か月の棒グラフ）と平均の点線（明細のある月で割る）。棒をタップでその月の金額を表示（明細には移らない）
    - カテゴリ別の推移（`catTrend`）。カテゴリのボタンを選ぶとそのカテゴリの12か月の棒グラフと平均線。棒をタップでその月の金額を表示
    - **予算の線**（`trendChart` の `budgets`）：予算を設定しているとき、**折れ線グラフ**（利用者の希望）で重ねる。
      列の中央を結ぶ SVG の `polyline`（`.bline`。`preserveAspectRatio="none"` + `vector-effect:non-scaling-stroke`）と、各月の点（`.bdot`、`--budget`）。予算のない月で線を切る。
      月別の支出・年の表示は月の予算、カテゴリ別の推移はその月の配分額（`budgetAlloc`。配分のない月は線なし）。
      予算を超えた月の棒は赤（`.col.over`）。吹き出しは2行で「予算 ¥…・残り ¥…（超えたら ▲¥…）」。グラフの下に凡例「予算／平均」
    - よく使ったお店（`shopPanel`）
  - 年の表示（`renderYear`）：`S.month` の年の1〜12月をまとめる
    - 帯は「前年｜その年の支出｜次年」。左右のタップ・スワイプで1年ずつ（`shiftYm(±12)`。月の表示に戻ると同じ月）。中央をタップで年のプルダウン（`yearOptions`。年だけを差し替える）
    - 予算は「明細のある月の数 × 月の予算」と比べる（`予算（◯か月分）`）。カテゴリ別の予算は各月の配分の合計
    - 件数・月平均（明細のある月で割る）・前年同期比（今年データのある月と同じ月どうしで比べる）
    - カテゴリ別・その年の月ごとの支出（平均線つき）・よく使ったお店
  - 明細に移るのは、カテゴリ別（ドーナツと一覧）と未分類のボタンからだけ。棒グラフのタップは金額の吹き出し（`showTip`）を出すだけ
  - 概要から明細に移ったときは、ヘッダーの右上に「‹ 概要」ボタン（`#back-ov`、`S.backOv`）。概要を離れたときのスクロール位置（`S.ovY`）を覚えておき、
    戻るボタンでも下のタブでも、概要に戻ったら元の位置に戻す（`goTab`）
- **明細**（`renderList`）: 月の明細一覧、カテゴリ変更、検索、手入力、削除（元に戻す付き）。上部に「‹ 2026年8月 ›」の月切り替え（月の部分をタップでプルダウン `#mhead`）
  - **カテゴリ変更はプルダウンではなくシート**（`renderSheet`、`S.pick`）。iPhoneの回転式プルダウンで隣の項目を選んでしまう誤操作があったため。
    行のボタン（`.cpick`）を押すと、下から大きなボタン（3列、アイコンつき）の一覧が出る。「未分類」は誤って選ばないよう最後。幕・「閉じる」・Escで閉じる。出すときの動きは付けない
  - カテゴリを変えると、トーストに「元に戻す」が出る（`setCat`）。「覚える」で一緒に変わった同じ店の明細とルールも元に戻る
- **取り込み**（`renderImport`）: CSVファイルを選んで取り込み、結果を表示
- **設定**（`renderSettings`）: バックアップ（書き出し・読み込み・最後に書き出した日）、予算（`budgetPanel`）、カテゴリ（名前・アイコン変更、追加、削除）、自動分類ルール、データの初期化

データがないときは架空のサンプルデータ（`makeSample`）を表示し、バナーで知らせる。サンプルは保存しない。

**予算**（利用者と決めたこと）：超えた金額は「オーバー」と書かず、前月比と同じ「▲¥…」で表す。入れるのは**毎月同じ合計金額だけ**。カテゴリ別は自動で配分する（`budgetAlloc`）。
- 家賃など決まった金額のカテゴリは、設定で金額を入れると固定できる（空欄は自動）。固定分を先に取り、残りを配分する
- 配分は**直近3か月**（その月を含まない）の支出の割合。未分類と削除済みのカテゴリは除く。100円単位で、端数はいちばん多いカテゴリへ
- 直近3か月に明細がない月は、固定した分だけが配分される
- カテゴリ別の一覧の各行に、細いバーと「残り ¥… / ¥…」（超えたら赤で「▲¥…」）。予算があってまだ使っていないカテゴリも一覧に出す

**バックアップのお知らせ**（`backupBanner`）：保存先が `localStorage` で、サンプルでなく、まだ書き出していないか最後の書き出しから30日以上たったら、概要にバナーを出す。「あとで」で7日間出さない。

## 動き・操作の方針（利用者と決めたこと）

- 残している動きは**2つだけ**：グラフが伸びる（`#view.grow` / `.trend.grow`）と、合計金額のカウントアップ（`countUp`）。
  画面切り替えのスライド、タップで沈む・跳ねる動きは**利用者の希望で削除済み**。勝手に戻さない。
- **スワイプで月を切り替える**（概要・明細タブのみ）。左スワイプで次月、右スワイプで前月。
  - 縦スクロールと誤反応しないこと（縦の動きが少しでも勝ったら縦スクロール扱い）
  - 動きは控えめ：指について最大56pxだけ動く。薄く（半透明に）しない。画面の外へ流さない
  - 画面の左右の端（24px）から始まるスワイプは無視（OSの操作と競合させない）
  - スワイプでタブを切り替える機能は、月の切り替えと競合するので**やめた**
- `prefers-reduced-motion` が有効なときは動きを出さない。
- ヒートマップ（色の濃さの表）は「見にくい」ため不採用。推移は棒グラフで表す。

## 見た目の方針（利用者と決めたこと）

- 「カード型・すっきり」：カードは枠線なしで、やわらかい影（`--shadow`）と大きめの角丸（`--radius` 18px / `--radius-sm` 12px）
- 下のタブは半透明のすりガラス（`--tabbar` + `backdrop-filter`。使えないときは `--surface` で塗る）。選んでいるタブはアイコンの後ろにピル型の背景
- 入力欄は `--surface-2` の塗りで枠なし、フォーカスで `--accent` の枠。主なボタン（`.btn.primary`）はピル型
- 棒が9本以上のグラフは、目盛りを「月」なしの数字だけにする（重なり防止）
- 棒グラフの今の月以外の色（`--bar-dim`）は、「薄すぎる」と言われたので中間の濃さ（ライト `#86b8a6`／ダーク `#3f6f5f`）。
  予算を超えた月（今の月以外）は `--danger-dim`、今の月は `--danger`

## スマホ向けの実装上の注意

- 入力欄・プルダウンの文字は**16px以上**（iPhoneで入力時に画面が拡大されるのを防ぐ）。
- ノッチやホームバーの領域は `env(safe-area-inset-*)` で避ける。下部タブもこれを考慮済み。
- **画面の横幅を超えるものを作らない。** グリッドの子要素は `min-width:0`、列は `minmax(0,1fr)`。
  過去に、横スクロールするボタン列がグリッドを押し広げて画面全体が縮小表示になる不具合があった。
- `[hidden]{display:none!important}` を使っている。表示の切り替えは `el.hidden` で行う。
- **入力中の欄を作り直さない。** 検索（明細の `#q`、ルールの `#rq`）は結果の部分（`listResults` / `ruleRows`）だけ差し替える。
  入力欄ごと `render()` し直すと、日本語入力の変換中の文字が1文字ずつ確定されてしまう（過去の不具合）。変換中（`isComposing`）は待ち、`compositionend` で反映する。
- 横スクロールする部分（`.chips` など）の上ではスワイプの判定をしない。
- ダークモード対応：色はすべて `:root` のCSS変数（トークン）で指定し、ダーク用は `prefers-color-scheme` と `[data-theme="dark"]` の両方で上書きする。

## データ形式（localStorage）

- `kkb-data`: `{ settings, months }`
  - `settings`: `{ v: 3, cats: [...], rules: [...], budget: { total, fixed } }`
    - `budget`（v3で追加）: `total` は月の予算（0なら使わない）、`fixed` は `{ カテゴリid: 金額 }`（手で固定した分）。
      無いとき（v2以前・古いバックアップ）は `normBudget` で「予算なし」にする。書き出すバックアップにも `budget` を入れる
    - `cats`: `{ id, name, icon }` の配列。`icon` は `ICONS` のキー（`null` なら `DEFAULT_ICON[id]`、なければ `tag`）
    - `rules`: `{ kw, cat }` の配列。お店の名前に `kw` が含まれていれば `cat` に分類
  - `months`: `{ "2026-08": [明細...] }`（利用日の年月ごと）
    - 明細: `{ id, date:"YYYY-MM-DD", name, raw, amount, cat, src:"csv"|"manual", memo?, locked? }`
    - `locked: true` は利用者が手でカテゴリを選んだもの。ルールの再適用で上書きしない
    - `cat` が `"none"` は未分類
- `kkb-tab`: 最後に開いていたタブ（便利用、なくても動く）
- `kkb-ov`: 概要の「月｜年」の選択（`"month"` / `"year"`。便利用）
- `kkb-backup-at`: 最後にバックアップを書き出した日（`YYYY-MM-DD`）
- `kkb-backup-snooze`: バックアップのお知らせで「あとで」を押した日（`YYYY-MM-DD`）

`localStorage` の読み書きは必ず `try/catch` で囲む。

### 保存形式を変えるとき

`settings.v` を上げ、`applyLoaded` の後で1回だけ走る移行関数を追加する（例: `migrateV2`。v3は項目の追加だけなので `normBudget` で既定値を入れている。
「外食」を「食費」に統合し、「衣服・美容」を「衣服」と「美容」に分けた処理）。
利用者が名前を変えているカテゴリなど、手を加えた部分は勝手に上書きしない。
バックアップ（書き出したJSON）の読み込みでも同じ移行が効くようにする（`restore-ok` で `migrateV2()` を呼んでいる）。

## カテゴリと自動分類

- 既定のカテゴリ（`DEFAULT_CATS`、14個）:
  食費・日用品・交通費・住居費・水道光熱費・通信費・サブスク・趣味・娯楽・医療・健康・衣服・美容・交際費・教養・書籍・その他
  - 「外食」は作らない（食費に含める）。衣服と美容は分ける
- 既定のルールは `DEFAULT_RULES`。一般的なチェーン店・サービス名だけを入れる（利用者個人の明細から店名を足さない）
- 照合は `mkey()` で表記ゆれを吸収してから行う：NFKC正規化（半角カナ→全角、全角英数→半角）、小文字化、
  ひらがな→カタカナ、小さい文字→大きい文字（ョ→ヨ。カード明細の表記に合わせる）、長音とハイフンの統一、空白・記号の除去
- 複数のキーワードに当てはまるときは**長いキーワードが優先**（`classify`）。例：「ガス」より「ガスト」
- 明細でカテゴリを変えると（「変更したお店を覚える」がオンなら）、その店名をルールに追加し、同じ店の未固定の明細も変更（`setCat`）
- 2文字程度の英字キーワード（例: `au`、`gu`）は他の店名に誤って当たりやすいので入れない

## カテゴリのアイコン

- 色付きの丸に白い線画（`ICONS` に `[色, SVGのpath]` で定義。24×24のviewBox、線で描く）
- 設定画面で丸をタップすると一覧（`PICKABLE`）から選べる。色はアイコンごとに固定
- 未分類は `question`（オレンジ）で、一覧には出さない
- アイコンを追加するときは、白い線がはっきり見える濃さの色にする

## CSVの取り込み（楽天カード）

- 文字コードはUTF-8（BOM付き）。読めなければShift_JISで再試行（`readText`）
- 見出し行の「利用日」「利用店名・商品名」「利用金額」（なければ「支払総額」）の列を名前で探す（`parseRakuten`）
- 利用日が空の行（海外利用の「現地利用額…」）は、直前の明細の `memo` にする
- 明細のIDは「利用日|店名|金額|同じ組み合わせの何件目か」のハッシュ。同じCSVを何度取り込んでも重複しない
- 表示用の店名は `disp()`（NFKC、`利用国JPN` などの除去、カタカナの間の `-` を `ー` に。ただし「ン」「ッ」の後は除く）

## 動作確認のしかた

- ローカルで `python3 -m http.server` などで配信し、スマホ幅（例: iPhone 13 の 390×664）で確認する
- 確認すること
  - コンソールにエラーが出ていない
  - `document.documentElement.scrollWidth` が画面幅と同じ（横にはみ出していない）
  - 4つのタブ・月の切り替え・スワイプ・グラフのタップが動く
  - ライト・ダーク両方で文字が読める
- 実際の明細データでは確認しない（架空のデータかサンプルを使う）

## 文章・表記

- 画面の文言はすべて日本語。利用者から見た言葉で書く（例：「CSVファイルを選ぶ」「元に戻す」）
- 金額は `yen()`（`¥1,234`）。数字は等幅・桁揃え（`.num`）
- エラーメッセージは、何が起きたかとどうすればよいかを書く
